---
title: 5.2 工具链与资源准备
chapter: 5
keywords: toolchain, environment, dependencies, API keys, RAG
version: 5.2
---

# 5.2 工具链与资源准备

> **本节学习目标**：掌握记忆系统开发所需的完整工具链配置，确保开发环境一致性和安全性

---

## 5.2.1 工具链概览

本章开发的记忆系统涉及4类工具：

| 类别 | 说明 | 本章涉及工具 |
|------|------|------------|
| 🛠️ 开发环境 | 代码编写与构建 | JDK 17+、IDE、Maven |
| 🔑 密钥与配额 | API访问凭证 | OpenAI、通义千问、本地模型 |
| 📦 依赖清单 | Java库依赖 | agentscope-java、Chroma、SentenceTransformers |
| 🔍 调试观测 | 问题排查与性能分析 | SLF4J、JMH、Mockito |

---

## 5.2.2 详细工具清单（Markdown表格）

### 5.2.2.1 🛠️ 开发环境

| 工具/资源名称 | 用途 | 版本要求 | 获取成本 | 获取路径/命令 | 是否必需 |
|-------------|------|---------|---------|--------------|---------|
| **JDK** | Java编译与运行环境 | ≥17 | 免费 | [Adoptium](https://adoptium.net) 或 `sdk install java 17-tem` | ✅ 必需 |
| **IDE** | 代码编辑与调试 | IntelliJ IDEA/Eclipse | 免费（社区版） | [JetBrains](https://www.jetbrains.com/idea/download) | ✅ 必需 |
| **Maven** | Java依赖管理 | ≥3.8 | 免费 | [Maven官网](https://maven.apache.org/download.cgi) | ✅ 必需 |
| **Git** | 版本控制 | ≥2.30 | 免费 | `git --version` 或 [Git官网](https://git-scm.com/download) | ✅ 必需 |

### 5.2.2.2 🔑 密钥与配额

| 工具/资源名称 | 用途 | 版本要求 | 获取成本 | 获取路径/命令 | 是否必需 |
|-------------|------|---------|---------|--------------|---------|
| **OpenAI API Key** | 云端LLM调用（GPT-4/Claude） | 最新版 | 按调用收费（¥0.6/百万tokens） | [OpenAI Platform](https://platform.openai.com/api-keys) | ⚠️ 可选（推荐教育优惠） |
| **通义千问API Key** | 云端LLM调用（qwen-max） | 最新版 | 按调用收费（¥2.4/百万tokens） | [阿里云百炼](https://bailian.console.aliyun.com/api-key) | ⚠️ 可选（教育优惠：¥100/年额度） |
| **Ollama** | 本地LLM运行（Qwen2/Llama3） | ≥0.1.0 | 免费（仅消耗本地资源） | `curl -fsSL https://ollama.com/install.sh \| sh` | ✅ 推荐（本地开发） |
| **vLLM** | 本地LLM高性能服务 | ≥0.2.0 | 免费 | `pip install vllm` | ✅ 推荐（生产环境） |

### 5.2.2.3 📦 依赖清单（Maven）

| 库名 | Maven坐标 | 版本 | 用途 | 官方文档URL |
|-----|----------|------|------|------------|
| **agentscope-java-spring** | `com.alibaba.agentscope:agentscope-spring` | `0.2.0` | Agent框架集成 | [agentscope.io](https://agentscope.io) |
| **Chroma Java Client** | `io.chroma:chroma-java-client` | `0.1.0` | 向量数据库客户端 | [trychroma.com](https://docs.trychroma.com) |
| **Sentence Transformers** | `org.allenai.sbert:all-minilm-l6-v2` | `2.2.0` | 文本向量化 | [sbert.net](https://www.sbert.net) |
| **Spring Boot Starter** | `org.springframework.boot:spring-boot-starter` | `3.2.0` | Spring Boot核心 | [spring.io](https://docs.spring.io/spring-boot/docs/current/reference/htmlsingle) |
| **Jackson** | `com.fasterxml.jackson.core:jackson-databind` | `2.15.0` | JSON序列化 | [fasterxml.com](https://github.com/FasterXML/jackson) |
| **Mockito** | `org.mockito:mockito-core` | `5.8.0` | 单元测试Mock | [mockito.org](https://site.mockito.org) |

### 5.2.2.4 🔍 调试观测

| 工具/资源名称 | 用途 | 版本要求 | 获取成本 | 获取路径/命令 | 是否必需 |
|-------------|------|---------|---------|--------------|---------|
| **SLF4J + Logback** | 日志记录与查看 | ≥1.7 | 免费 | `org.slf4j:slf4j-api` + `ch.qos.logback:logback-classic` | ✅ 必需 |
| **JMH** | 性能基准测试 | ≥1.35 | 免费 | `org.openjdk.jmh:jmh-core` | ✅ 推荐 |
| **Postman** | HTTP请求抓包 | 最新版 | 免费（基本版） | [postman.com](https://www.postman.com/downloads) | ✅ 推荐 |
| **Mockito** | Mock外部依赖 | ≥5.0 | 免费 | `org.mockito:mockito-core` | ✅ 必需 |

### 5.2.2.5 🌐 官方资源

| 资源名称 | 用途 | URL | 注意事项 |
|---------|------|-----|---------|
| **AgentScope-Java文档** | API参考 | [agentscope.io/docs](https://agentscope.io/docs) | v0.2.0为当前稳定版 |
| **AgentScope-Java GitHub** | 源码与Issue | [github.com/modelscope/agentscope-java](https://github.com/modelscope/agentscope-java) | 提交Issue前搜索已有问题 |
| **Chroma文档** | 向量数据库操作 | [docs.trychroma.com](https://docs.trychroma.com) | 支持本地与云端部署 |
| **Spring Boot文档** | 框架使用 | [docs.spring.io/spring-boot](https://docs.spring.io/spring-boot/docs/current/reference/htmlsingle) | 重点阅读"Configuration Properties"章节 |
| **OpenAI API文档** | API调用规范 | [platform.openai.com/docs](https://platform.openai.com/docs) | 注意Token计数规则 |
| **通义千问API文档** | API调用规范 | [help.aliyun.com/document_detail/611860.html](https://help.aliyun.com/document_detail/611860.html) | 教育优惠需认证学生身份 |

---

## 5.2.3 🚨 安全规范（必须遵守）

### 5.2.3.1 禁止硬编码API Key

**❌ 错误示例**：
```java
// ❌ 不要这样做！（除非仅用于本地测试）
public class APIConfig {
    private final String apiKey = System.getenv("OPENAI_API_KEY"); // 从环境变量读取
}
```

**✅ 正确示例**：
```java
// ✅ 使用环境变量（开发环境）
@Value("${openai.api.key}")
private String apiKey;

// 或使用System.getenv()（生产环境）
private final String apiKey = System.getenv("OPENAI_API_KEY");
```

### 5.2.3.2 配置文件安全

**`.env`文件模板**（**切勿提交到Git**）：
```env
## .env（添加到.gitignore）
OPENAI_API_KEY=your_openai_api_key_here
DASHSCOPE_API_KEY=your_dashscope_api_key_here
CHROMA_ENDPOINT=http://localhost:8000
```

**`.gitignore`配置**：
```gitignore
## 忽略包含密钥的文件
.env
.env.local
.env.*.local
```

### 5.2.3.3 使用dotenv-java加载配置

```java
// 添加依赖
// <dependency>
//     <groupId>io.github.cdimascio</groupId>
//     <artifactId>dotenv-java</artifactId>
//     <version>3.0.0</version>
// </dependency>

public class ConfigLoader {
    public static String load(String key) {
        Dotenv dotenv = Dotenv.configure()
            .directory(".env") // 指定.env文件目录
            .ignoreIfMissing()
            .load();
        return dotenv.get(key);
    }
}
```

---

## 5.2.4 环境验证清单

完成以下检查后，确认环境配置正确：

| 检查项 | 命令/操作 | 预期结果 |
|-------|---------|---------|
| JDK版本 | `java -version` | ≥17（如：openjdk version "17.0.9"） |
| Maven版本 | `mvn -version` | ≥3.8（如：Apache Maven 3.8.8） |
| Git配置 | `git config --global user.name` | 已设置用户名 |
| OpenAI API Key | `echo $OPENAI_API_KEY`（Linux/Mac）或 `echo %OPENAI_API_KEY%`（Windows） | 返回非空字符串 |
| 本地Ollama | `curl http://localhost:11434/api/tags` | 返回JSON格式的模型列表 |

---

## 5.2.5 快速开始脚本（Windows/Linux）

```bash
## setup.sh / setup.bat（根据系统选择）
## 安装JDK 17（使用sdkman）
sdk install java 17-tem

## 安装Maven（Linux）
sudo apt-get install maven

## 启动Ollama（本地LLM）
ollama serve &

## 启动Chroma（向量数据库）
docker run -p 8000:8000 chromadb/chroma:latest

## 初始化项目（Maven）
mvn archetype:generate \
    -DgroupId=com.textbook.chapter5 \
    -DartifactId=memory-system \
    -DarchetypeArtifactId=maven-archetype-quickstart \
    -DinteractiveMode=false
```

---

## 5.2.6 本节思考题

**问题1**：为什么不能将`.env`文件提交到Git仓库？  
**提示**：思考密钥泄露的风险（如：GitHub扫描、仓库公开）  

**问题2**：如果公司禁止使用云端API，如何选择本地替代方案？  
**提示**：对比Ollama与vLLM的性能、易用性、资源消耗  

**问题3**：`.env`与`application.yml`的区别是什么？  
**提示**：思考开发环境与生产环境的配置管理差异  

---

## 5.2.7 下节预告

下一节将介绍**短期记忆与长期记忆的核心概念**，包括：
- `ShortTermMemory`：LinkedList + 固定窗口设计
- `LongTermMemory`：JSON持久化 + Redis缓存
- `RAG`：向量检索 + 语义相似度匹配

---

> 本节预计学习时间：20分钟  
> ✅ 完成标准：能独立配置.env文件并加载API Key  
> 📖 下一节：5.3 核心概念
